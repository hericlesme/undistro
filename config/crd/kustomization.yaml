apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- bases/getupcloud.com_providers.yaml
#- bases/getupcloud.com_metadata.yaml excluding metadata from the CRD manifest generation because metadata will be used as a ComponentConfig file only

- bases/getupcloud.com_clusters.yaml
# +kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD

- patches/webhook_in_clusters.yaml
# +kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable webhook, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD

- patches/cainjection_in_clusters.yaml
# +kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
